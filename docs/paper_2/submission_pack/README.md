# Paper 2 — Submission Pack (v3, ECM rework)

**Living index. Last updated 2026-09-02.** Everything here is the CURRENT (v3, atmospheric)
state. Superseded v1/v2 material is in `../_archive/`.

## Layout

| Folder | Contents | Status |
|---|---|---|
| `00_prompts/` | `PAPER2_ECM_PLAN_v3.md` (**source of truth**), `AGENT_PROMPT_Paper2_v3.md` | current |
| `00_statements/` | v3 findings + cumulative technical record (see below) | current |
| `01_figures/` | figures | ⚠ **pre-v3 (Aug 30)** — being regenerated by the v3 campaign |
| `02_tables/` | tables | ⚠ **pre-v3 (Aug 30)** — being regenerated |
| `03_data/` | KPI/sweep CSVs | ⚠ **pre-v3 (Aug 30)** — being regenerated |
| `04_scripts/` | figure/table generators (snapshot) | to be updated for v3 |
| `_dropped/` | figures dropped from the pack | archive |

## 00_statements (read these first)

- **`WP0_BEFUND.md`** — the diagnostic that reframed the rework: the HK↔storage coupling was
  already in the model; hourly grid-CO₂ already used; the real lever is storage technology/cost.
- **`PHASE1_STORAGE_COST_PARAMS.md`** — atmospheric pit/tank cost curve (b=0.657, Danish PTES fit).
- **`F6_FUTURE_EF_APPROACH.md`** — method for the 2030/2045 EF scenarios (F6).
- **`CHANGELOG_v3.md`** — every config/code change with before→after + source.
- **`CALION_Paper2_Implementation_Statement.md`** — cumulative technical record (Parts A–I).

## Where the paper stands (2026-09-02)

- **Phase 0 (diagnosis):** ✅ complete — reviewer thesis validated with a corrected mechanism.
  Both networks show an inner storage optimum from value saturation (~7–11 h of mean load).
- **Phase 1 (atmospheric model):** ✅ built + verified. `geometric_storage.py` gains surface
  loss, degressive cost, η_strat, ~95 °C ceiling — behind opt-in flag `CALION_ATMOSPHERIC_TES=1`
  (legacy bit-for-bit safe). Params in `configs/paper_2/storage_geometry.yaml`.
  Verified interior optimum (Memmingen ~2 MWh, +€24.5k/a). Key finding: storage value is largely
  unit-commitment/cycling smoothing → sizing runs must keep commitment ON.
- **Phase 2 (campaign → figures):** ⏳ in progress. **Study G** (sizing, F2) running:
  fine storage-hour-anchored ladders, commitment ON, atmospheric. Results land in
  `results/value_probe_*` then flow here as the new F2.

## Figures — v3 plan (from PLAN_v3)
F1 networks · **F2 sizing/geometry (centrepiece)** · F3 heat-curve coupling · F4 siting ·
F5 aggregated-vs-nodal · F6 break-even (CO₂×price, real hourly EF) · F7 tornado.
F1 map sources: `../figures/` (Memmingen_topology.png, Stadtbach_Topology.pdf).

## Reproduce (v3)
- Atmospheric model: set env `CALION_ATMOSPHERIC_TES=1`; params in `configs/paper_2/storage_geometry.yaml`.
- Legacy (frozen v1/v2 results): leave the flag unset.
