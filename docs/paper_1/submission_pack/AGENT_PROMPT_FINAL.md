# Final assembly prompt — copy everything below into the coding agent

---

Finish the Applied Energy revision (APEN-D-26-15734). The manuscript is assembled and
compiles cleanly at 25 pages; what remains is one content pass, the citation fixes, the
response letter, and the two output builds.

**Ground rules**

- `paper_source_skeleton.tex` is the source. `paper_COMPILE.tex` is generated by
  `fill_paper.py` — never edit it directly; edits there are lost on regeneration.
- `tab_prior_work.tex` is likewise generated, by `tools/tablegen_p1.py`. Fix the generator.
- `cas-dc` does not load `siunitx` or `mhchem`. Use plain units (`CO$_2$`, `°C`, `2.5\,K`,
  `\,\%`). Do not introduce `\SI{}{}`, `\si{}` or `\ce{}`.
- Three edits were already made in the Desktop mirror
  (`C:\Users\LKR\Desktop\submission_pack\submission_pack\...`) and may not be in the repo:
  the two VSS entries appended to `Paper20_Literatur.bib`, the `\citep{BirgeLouveaux2011}`
  in `sections/related_work_v2.tex`, and the same citation in the skeleton's contributions
  paragraph (§1.2, the sentence beginning "We position the regret idea honestly"). Check
  and re-apply if absent.

---

## Step 1 — Citation keys (blocking; takes undefined citations to zero)

In `tools/tablegen_p1.py`, around lines 604–608, four hard-coded keys do not exist in the
bibliography. Replace them, then regenerate the table:

| Wrong | Correct |
|---|---|
| `Wirtz.2021` | `Wirtz2021_complexity` |
| `Kotzur.2021` | `Kotzur2021` |
| `vanderHeijde.2019` | `vanderHeijde2017` |
| `Hering.2021` | `Hering2021` |

Note `vanderHeijde2017` is a 2017 entry — if any prose says "2019" for that reference,
correct it.

Then delete `paper1_dh_fidelity/vss_citation.bib`; its two entries are now merged into
`Paper20_Literatur.bib` and a single `\bibliography` line resolves everything.

## Step 2 — Content pass

Apply `03_draft_revision/02_revision_hand_in/FOLLOWUP_PATCHES.md` to the skeleton. Nine
items, each with an exact anchor including the `\result{}` macros. In priority order:

1. **§1 and §2 — the conditional topology bound.** Appears in three places (abstract,
   estimation-bias subsection, generalisability subsection) plus `tab_decomposition`.
   Currently "±2.38 % on every network"; should be "±0.6 % on every network of 5 km trunk
   length or more, never exceeding 2.4 % even on the shortest, where the entire gap is below
   6 % of cost". More accurate and a stronger claim.
2. **§3 — out-of-sample prose.** If the text still says "a couple of percentage points" and
   "≈14 %", it contradicts `tab_prediction` on the same page (7 pts, 20 pts, 19 %).
3. **§1 abstract reorder and the "not ground truth" sentence** (§5.2, §5.3).
4. **§4 fidelity-rule residual**, **§5 moderator trim**, **§6 conclusions**, **§7 front
   matter** — wire `sections/conclusions_v2.tex` and `sections/front_matter_v2.tex`.
5. **§8** — drop the `heating-curve` marker; keep `l2-zones` if space allows.

## Step 3 — Response letter

Apply `03_draft_revision/02_revision_hand_in/RESPONSE_LETTER_DELTAS.md`:

- the old→new section-pointer map for all thirteen reviewer items;
- the substantive corrections (topology bound, drift wording, the methods note, R2.5's
  filtering answer, R1.6's data answer, the proactive disclosures);
- the **held-out-split amendment** — R2.4's response still promises a fitted/held-out node
  split that `validation_spatial.py` has since shown is unsupportable; replacement text is
  in the file;
- the **level-nomenclature bridge table** near the top. The reviewers read the letter with
  v1 open, where `L1` means copperplate and `L3` means what we now call `L1`. Without it,
  R1.2's "13 % between L1 and L3" looks unanswered.

Fill the final Table and Figure numbers from the compiled PDF.

## Step 4 — Build and verify

```
python tools/fill_paper.py --auto --src <skeleton> --out <compile>
pdflatex → bibtex → pdflatex → pdflatex
```

Do not stop until **all four** are zero: undefined control sequences, undefined
cross-references, undefined citations, missing files. Report the page count.

## Step 5 — Markup and clean PDFs

Per `03_draft_revision/02_revision_hand_in/MARKUP_BUILD_NOTES.md`. Add the `\ifmarkup`
toggle and the three colour macros to the preamble, build twice from the **same** source —
`\markuptrue` → `paper_MARKUP.pdf`, `\markupfalse` → `paper_CLEAN.pdf`.

Wrap at **paragraph** granularity, not sentence: `\new` for the fifteen merged section files
and the four new subsections, `\chg` for transcribed v1 material and numbers that moved with
the 135-network grid, `\gone` for exactly three removals (the BCM cross-check, v1's
linearisation/delay confound limitation, the physics-scope mapping table). Include the boxed
legend from the notes file — it explains the paragraph granularity before an editor asks.

## Step 6 — Housekeeping

- The Desktop pack has both `submission_pack/old/` and a nested
  `submission_pack/submission_pack/`. Confirm which is live and flatten; nobody should be
  able to patch the wrong copy.
- Sync the final skeleton, compiled `.tex`, both PDFs, tables, figures and the response
  letter to the Zenodo release.

---

## Done means

- [ ] Undefined citations, references, control sequences, missing files: all zero
- [ ] `paper_CLEAN.pdf` and `paper_MARKUP.pdf` built from one source
- [ ] Response letter has correct pointers, the bridge table, and the held-out amendment
- [ ] `front_matter_v2` and `conclusions_v2` wired
- [ ] No `<<KEEP:>>`, `<<GAP:>>` or `<<CITE-NEEDED:>>` markers remain in the body
- [ ] Zenodo synced

Outstanding for the author, not the agent: the competing-interest text, and the graphical
abstract re-export per `GRAPHICAL_ABSTRACT_SPEC.md`.
