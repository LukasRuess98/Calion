# =============================================================================
# PRESET: Quick Test
# =============================================================================
# Fast development/testing preset
# - 1 week horizon
# - Baseline system (no HP, no storage)
# - Fast solver settings
#
# Usage: python -m energis.run configs/presets/quick_test.yaml
# =============================================================================

# Inherit from base configs (evaluated in order)
_extends:
  - ../00_base/solver.yaml
  - ../00_base/costs.yaml
  - ../00_base/grid.yaml
  - ../01_tech/fuels.yaml
  - ../01_tech/generators.yaml
  - ../02_site/stadtbach/data_source.yaml
  - ../02_site/stadtbach/assets.yaml
  - ../03_systems/baseline.yaml
  - ../04_scenarios/test_1week.yaml

# No additional overrides needed - all settings come from parents
