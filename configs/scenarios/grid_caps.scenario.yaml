scenario:
  title: Grid caps demo
  run_mode: PF_ONLY
  workflow:
    - PF
  fix_design: false
  rolling_horizon:
    heat_horizon_hours: 168.0
    step_hours: 24.0
    terminal_policy: free

grid:
  max_import_mw: 5.0
  max_export_mw: 2.0
