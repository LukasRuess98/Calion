# =============================================================================
# SOLVER CONFIGURATION
# =============================================================================
# Solver settings for optimization
# Available solvers: glpk (open-source), cbc (open-source), gurobi (commercial)
# =============================================================================

run:
  dt_h: 1.0
  solver: cbc            # CBC is faster than GLPK for MILP problems

  # CBC options (leave empty for defaults)
  solver_options: {}
