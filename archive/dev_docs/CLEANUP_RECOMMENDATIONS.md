# Cleanup & Obsolete Files Report
**Date:** 2026-03-27

---

## 1. HiGHS Solver Compatibility ✅

**Status:** Fully compatible with export system

**Verification:**
- Config uses `solver: appsi_highs` ✅
- Export function is solver-agnostic (uses Pyomo `model.write()`) ✅
- File naming (`gurobi_solution.*`) is just convention - works with all solvers ✅
- All 5 export types (LP, SOL, MPS, CSV, manifest) generate successfully with HiGHS ✅

**Recent test run (yearly 8,760 hours):**
```
✅ Solver: HiGHS 1.13.1
✅ Status: Optimal (0% gap)
✅ Export: 5 files (30 MB total)
   - LP: 13.69 MB
   - MPS: 14.67 MB
   - SOL: 1.36 MB
   - CSV: 0.20 MB
   - Manifest: <1 KB
```

---

## 2. Obsolete & Cleanup Candidates

### 🗑️ **SAFE TO DELETE** (Large, Rebuild-able)

| Item | Size | Type | Status | Notes |
|------|------|------|--------|-------|
| `HeatGrid/` | **440 MB** | Python venv | ❌ Remove | Use system Python or Poetry instead |
| `htmlcov/` | **13 MB** | Coverage reports | ❌ Remove | Regenerate with `pytest --cov` |
| `.pytest_cache/` | ~1 MB | Cache | ❌ Remove | Auto-recreated by pytest |
| `energis.egg-info/` | ~1 MB | Build artifact | ❌ Remove | Regenerated by `pip install -e .` |
| `__pycache__/` (all) | ~2 MB | Python cache | ❌ Remove | Auto-created by Python |

### 🗑️ **LIKELY OBSOLETE** (Old Debug/Test Files)

**Root-level debug files (0-1 MB each):**
- `star_debug.lp` (3.18 MB) - Old solver output
- `debug_lp.lp` (1.22 MB) - Old solver output  
- `debug_model.lp` (1.22 MB) - Old solver output
- `level1_result.txt` - Old test result
- `level2_test.log` - Old test log
- `stadtbach_result.txt` - Old result file
- `test_output.log` - Old test log
- `coverage.xml` - Old coverage file

**Root-level diagnosis scripts:** (questionable value)
- `diagnose_phase1.py` through `diagnose_phase5.py`
- `diagnose_hypothesis_test.py`
- `debug_consumer_constraints.py`
- `check_demand.py`

### 🔹 **CONSIDER REMOVING** (Optional)

| Item | Size | Type | Decision |
|------|------|------|----------|
| `saved_workflows/` | ~1 MB | Historical data | Keep if you need workflow history, else remove |
| `test_exports/` | ~1 MB | Test data | Remove if not used (only `model_structure/`) |
| `exports/` subdir | N/A | Old export dir | Superseded by `outputs/` - remove if empty |

### ✅ **KEEP** (Important)

| Item | Type | Reason |
|------|------|--------|
| `outputs/` | Current results | Active optimization outputs |
| `notebooks/` | Documentation | Used for demos & analysis |
| `configs/` | Configuration | Required for all runs |
| `data/` | Input data | Test datasets |
| `energis/` | Source code | Core framework |
| `tests/` | Unit tests | Code quality checks |
| `docs/` | Documentation | API reference |

---

## 3. Cleanup Recommendations

### Option A: Aggressive Cleanup (~457 MB saved)
```powershell
# Remove all build artifacts and caches
Remove-Item HeatGrid -Recurse -Force                    # 440 MB
Remove-Item htmlcov -Recurse -Force                     # 13 MB  
Remove-Item .pytest_cache -Recurse -Force               # 1 MB
Remove-Item energis.egg-info -Recurse -Force            # 1 MB
Get-ChildItem -Recurse -Directory -Filter __pycache__ | Remove-Item -Recurse -Force  # 2 MB

# Remove old debug files (root)
Remove-Item *.lp -ErrorAction SilentlyContinue          # ~5 MB
Remove-Item level*.txt, level*.log -ErrorAction SilentlyContinue
Remove-Item debug*.py -ErrorAction SilentlyContinue
Remove-Item diagnose*.py -ErrorAction SilentlyContinue
Remove-Item check_demand.py, stadtbach_result.txt -ErrorAction SilentlyContinue
```

### Option B: Conservative Cleanup (~455 MB saved)
```powershell
# Just remove venv and caches (safest)
Remove-Item HeatGrid -Recurse -Force                    # 440 MB (⚠️ Venv - never needed in source)
Remove-Item .pytest_cache -Recurse -Force               # Auto-recreated
Get-ChildItem -Recurse -Directory -Filter __pycache__ | Remove-Item -Recurse -Force

# Keep htmlcov in case it's useful for coverage info
# Keep debug scripts in case they have analysis value
```

### Option C: Minimal Cleanup (~1 MB)
```powershell
# Just cache cleanup (always safe)
Remove-Item .pytest_cache -Recurse -Force
Get-ChildItem -Recurse -Directory -Filter __pycache__ | Remove-Item -Recurse -Force

# Occasionally: rm -r htmlcov (can be regenerated anytime)
```

---

## 4. Suggested Cleanup Plan

### Step 1: Always Safe
```powershell
# Remove build artifacts (never needed in source)
Remove-Item HeatGrid -Recurse -Force              # 440 MB venv
Remove-Item .pytest_cache -Recurse -Force         # pytest cache
Remove-Item energis.egg-info -Recurse -Force      # build artifact
Get-ChildItem -Recurse -Directory -Filter __pycache__ | rm -r
```

Result: **~444 MB freed**, nothing breaks

### Step 2: Clean Up Large Artifacts (Optional)
```powershell
Remove-Item htmlcov -Recurse -Force               # 13 MB (regenerate with pytest)
Remove-Item coverage.xml -Force                   # 0.4 MB
```

Result: **+13.4 MB freed**, easy to regenerate

### Step 3: Remove Obsolete Debug Files (Optional)
```powershell
# Old solver outputs
Remove-Item *.lp -ErrorAction SilentlyContinue    # ~5 MB
Remove-Item level*_result.txt, level*.log -ErrorAction SilentlyContinue
Remove-Item stadtbach_result.txt -ErrorAction SilentlyContinue

# Old diagnosis scripts (if not needed)
Remove-Item diagnose*.py, debug*.py -ErrorAction SilentlyContinue
Remove-Item check_demand.py -ErrorAction SilentlyContinue
```

Result: **~+5 MB freed**

---

## 5. File Organization Recommendations

### For future, consider adding to `.gitignore`:
```
# Python artifacts
HeatGrid/
*.egg-info/
dist/
build/

# Test/Cache
.pytest_cache/
htmlcov/
coverage.xml
__pycache__/

# Debug/temporary outputs
*.lp
*_result.txt
*.log
debug_*.py
diagnose_*.py
```

### Keep organized:
```
PROJECT_ROOT/
├── configs/              ✓ Version control
├── energis/              ✓ Version control  
├── tests/                ✓ Version control
├── docs/                 ✓ Version control
├── data/                 ✓ Version control (test data)
├── notebooks/            ✓ Version control
├── outputs/              ✗ Git ignore (results)
├── results/              ✗ Git ignore (optional export dir)
```

---

## 6. Verification After Cleanup

```powershell
# Verify everything still works
python -m energis.run configs/scenarios/stadtbach_baseline_2023.yaml

# Verify tests pass
pytest tests/

# Check exports still work
# → outputs/runs/thermal_network_results/
```

---

## Summary

| Cleanup Level | Space Freed | Risk | Recommendation |
|---|---|---|---|
| **Level 1** (Essential) | 444 MB | None ✅ | **Always do** |
| **Level 2** (Recommended) | 457 MB | Low ✅ | Do now |
| **Level 3** (Optional) | 462 MB | None ✅ | Do if not needing debug |

**Suggested action:** Run Level 2 cleanup (delete HeatGrid, htmlcov, caches, .egg-info)
